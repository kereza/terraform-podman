# Super Basic Podman Container (Nginx)

Configuration in this directory runs a super simple Nginx container on Podman via the ROOT user. No mounted directories and config files. No additional start arguments and no environment variables.