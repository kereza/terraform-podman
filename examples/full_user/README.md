# Full setup  (Prometheus running via dedicate user )

Configuration in this directory runs Prometheus. It runs via dedicated user (prometheus). It has mounted directories and config files. It has additional start arguments, but no environment variables. The config file is in the expected folder.