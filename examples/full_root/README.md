# Full setup  (Ghost blog platform running via ROOT )

Configuration in this directory runs Ghost container (Blog platform). It runs via ROOT user. It has mounted directories and config files. It has environment variables set. No additional start arguments. The config file is in the expected folder.